Firestore -> create -> Rules -> add below code

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}



google console :-
API and Services -> Google Cloud Firestore API (Need To Add Link. See Console one time)